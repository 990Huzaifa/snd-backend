import { Injectable } from "@nestjs/common";

@Injectable()
export class AnnouncementService {
    // This service will handle all operations related to announcements, such as creating, updating, deleting, and fetching announcements for the platform.
}